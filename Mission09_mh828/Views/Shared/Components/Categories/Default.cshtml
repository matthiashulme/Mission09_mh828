@{
}

@model IEnumerable<string>

<div class="d-grid">
    <a asp-controller="Home" asp-action="Index" asp-route-category="" asp-route-pageNum="1" class="btn btn-light">Home</a>
    <br />

    @*@foreach (string t in Model)
        {
            <a asp-controller="Home" asp-action="Index" asp-route-category="@t" asp-route-pageNum="1"
               class="btn @(ViewBag.SelectedCategory == t ? "btn-dark" : "btn-light")">@t</a>
        }*@

    <!-- DropDown Menu -->
    <div class="dropdown">
        <button class="btn btn-light dropdown-toggle" type="button" data-toggle="dropdown">
            Categories
            <span class="hamburger"></span>
        </button>
        <ul class="dropdown-menu">
            @foreach (string t in Model)
            {
                <li>
                    <a asp-controller="Home" asp-action="Index" asp-route-category="@t" asp-route-pageNum="1"
                       class="btn @(ViewBag.SelectedCategory == t ? "btn-dark text-white" : "btn-light")">@t</a>
                </li>
            }
        </ul>
    </div>
</div>

<footer>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
</footer>